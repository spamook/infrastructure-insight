version: '3.9'



services:
  load-balancer:
    image: nginx:1.25-alpine
    container_name: infra-lb
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./load-balancer/nginx.conf:/etc/nginx/conf.d/default.conf:ro
